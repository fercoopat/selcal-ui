import { memo } from "react";

export const MillTypesPage = memo(() => (
  <div className="p-6">
    <h1 className="text-2xl font-semibold">Tipos de Laminador</h1>
  </div>
));

export const ProfileTypesPage = memo(() => (
  <div className="p-6">
    <h1 className="text-2xl font-semibold">Tipos de Perfil</h1>
  </div>
));

export const PassGeometryTypesPage = memo(() => (
  <div className="p-6">
    <h1 className="text-2xl font-semibold">Tipos de Geometría de Pase</h1>
  </div>
));
